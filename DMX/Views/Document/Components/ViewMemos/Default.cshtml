@using DMX.DataProtection
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model IEnumerable<DMX.ViewModels.ViewMemosVM>;





<div id="PlaceHolderHere"></div>

<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary d-inline">Clients</h6>
        
            <button class="btn btn-sm btn-rounded  btn-outline-primary fa-pull-right" data-bs-toggle="ajax-modal" data-bs-target="#AddMemo" data-bs-url="@Url.Action("AddMemo")"><i class="fa fa-fw fa-plus-circle"></i>Add Memo</button>
     
        </div>
        <div class="card-body">
            <div class="table-responsive">
            <table class="table dataTables_wrapper">
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>Sender</th>
                        <th>Recipient</th>  
                     
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                      @foreach (var Memo in Model)
                {
                    <tr>
                        <td> @Memo.Title</td>
                        
                        <td>@Memo.From</td>

                        <td>@Memo.To</td>
                         
                           <td>
                            <div class="btn-group" role="group">
                                <button id="btnGroupDrop1" type="button" class="btn btn-sm btn-rounded btn-outline-primary dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    Select Action
                                </button>
                                <div class="dropdown-menu" aria-labelledby="btnGroupDrop1">
                                    <button type="button" data-bs-toggle="ajax-modal" data-bs-url="@Url.Action("EditClient") class="dropdown-item-custom" data-bs-target="#EditClient"><i class="fa fa-edit mr-3"></i>Edit</button>
                                    @* <button class="dropdown-item-custom" data-bs-toggle="ajax-modal" data-bs-url="@Url.Action("EditService", new { Id = @Encryption.Encrypt(Client.Id) })" data-bs-target="#ClientService"><i class="fa fa-edit mr-3"></i>Service</button>
                                *@                                   @*  <a class="dropdown-item-custom" asp-action="PrintProfile" asp-controller="Client" asp-route-Id="@Encryption.Encrypt(Client.Id)"><i class="fa fa-print mr-3"></i>Print</a>
                                *@                                   <button type="button" data-bs-toggle="ajax-modal" data-bs-url="@Url.Action("ClientProfile") class="dropdown-item-custom" data-bs-target="#ClientProfile"><i class="fa fa-user mr-3"></i>Profile</button>
                                    <form asp-action="DeleteClient" asp-route-Id="" onsubmit="return jQueryAjaxDelete(this);">
                                        <button type="submit" class="dropdown-item-custom"><i class="fa fa-trash mr-3"></i>Delete</button>
                                    </form>
                                </div>
                            </div>

                        </td>
                    </tr>
             }
                </tbody>
            </table>
       </div>
       </div>

    <div class="card-footer">
    </div>
</div>


